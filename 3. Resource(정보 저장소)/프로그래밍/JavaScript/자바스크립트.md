## 함수

==reduce 함수==

**정의**
- 배열의 각 요소들을 누적해서 하나로 반환

**파라미터**
1. callback function  
    - 다음 4가지의 인수를 가진다.
        1. **accumulator** - accumulator는 callback함수의 반환값을 누적합니다.
        2. **currentValue** - 배열의 현재 요소
        3. **index**(Optional) - 배열의 현재 요소의 인덱스
        4. **array**(Optional) - 호출한 배열
    - callback함수의 반환 값은 accumulator에 할당되고 순회중 계속 누적되어 최종적으로 하나의 값을 반환합니다.
2. initialValue(Optional)
    - 최초 callback함수 실행 시 accumulator 인수에 제공되는 값, 초기값을 제공하지 않을경우 배열의 첫 번째 요소를 사용하고, 빈 배열에서 초기값이 없을 경우 에러가 발생합니다.

**예제**
![[Pasted image 20231030200900.png]]

querySelector과 getElemtById 는 같은 기능을 한다.
하지만 쿼리셀렉터는 ("hello" h1) 이렇게 hello클래스 안에 있는 h1태그 내용을 가져오는 등
더 세세하게 지정할 수 있다.

===Math 함수===

![[Pasted image 20231109105346.png]]

==코테에서 자주 쓰는 함수==

**숫자를 거꾸로 정렬해서 리스트로 만들기.**
- EX) n = 35142
- return ( n+" " ) .split( " " ).sort(  ).reverse(  ).map( o => parseInt( o ))

**숫자 자리수 합하기**
- EX) n = 35142
- return ( n+' ' ).split( ' ' ).reduce(( a , b ) => a + parseInt( b ) , 0 ))

**문장 안에 변수 넣는 방법.**
- 문장을 물결키에 있는 따옴표로 감싸주고 ${변수}
- Ex) "Hello ${username}"

**repeat 함수**
- 문자를 반복해서 출력 가능.
- EX) "^".repeat(5) -> "^^^^^"

**slice 함수**
- 배열 또는 string의 값을 복사하여 생성 가능.
- EX) phoneNumber = "01049476889" / phoneNumber.slice(3,7) -> "4947"

**전개 연산자**
- 함수를 파라미터 안에 넣을 때 사용
- EX) arr = [1,2,3,4,5]의 최소값을 구하고 싶다면 
- const minNum = Math.min(...arr) 앞에 . . .을 붙이면 됨.

제일 작은 수 제거하기 까지 품. (11/24)
가운데 글자 제거하기 까지 품 (12/1)

**hover**
- 마우스 올려놓으면 효과 생김

### FlexBox
- 창의 크기에 따라 요서의 크기와 레이아웃이 알맞게 변하는 창.
