태그 빨리 생성하기
b누르고 Tab누르면 <b></b> b태그 생성됨.

html 기본 태그들 불러오기
! 누르고 Tab - 기초 태그들 자동 생성

VsCode에서 커맨드 팔레트 띄우기
Crtl + Shift + P - 커멘드 검색 가능

자동 포멧팅
Shift + Alt + F - 문자열 자동으로 맞춰줌
(설정에서 저장 시 자동 포멧팅도 가능)

html 주석
<! -- 글자 --> - 구간 주석
(Crtl + / 으로 사용 가능)

Alt 누른 채 선택하면 다중 선택 가능


![[Pasted image 20231208151041.png]]
**블록과 인라인 구조**
앵커 태그는 글자가 차지하는 공간만 사용하는 데에 반에, 문단은 한 줄을 모두 차지한다.

div를 통해서 그룹화하고 정리한다.

엔티티 코드
&로 시작 ~~ ;로 끝남.
위 구성으로 이루어진 코드를 통해 특수문자 입력 가능.
(부등호는 VsCode에서 입력이 어려우므로 엔티티코드를 통해 입력)


![[Pasted image 20231208163618.png]]
시멘틱 요소는 사실 div태그와 다를 바 없다.
다만, 태그 이름을 통해서 사용 목적을 알 수 있기에 사용.

## Emmit Syntax

![[Pasted image 20231208180437.png]]
태그 자식구조 생성

![[Pasted image 20231208180513.png]]
태그 더하기

![[Pasted image 20231208180905.png]]
태그 곱하기


## Table
![[Pasted image 20231208193020.png]]

table을 통해 하나의 테이블을 만들기.

thead로 테이블 머리 만들기
tr은 하나의 행이고 행 안의 요소는 th

tbody로 몸통 만들기
tr은 하나의 행이고 요소는 td

form 안에 input에서 name은 반드시 있어야 한다. 서버로 데이터를 전송할 때 사용되기 때문에.

![[Pasted image 20231212172757.png]]
Input에서 Radio나 체크박스를 통해 데이터를 보낼 때 value값을 지정해줘서 선택지마다 각각 다른 데이터를 전송하게 만들어줘야 한다.

### 헥사코드 읽는 법
![[Pasted image 20231213021335.png]]
0~F(0~255와 비슷)순으로 16가지로 표현.
![[Pasted image 20231213021553.png]]
각 2칸 씩 RGB순으로 해당

### CSS
css에서 태그 선택할 때
태그 = .name , id = #name



