****캐러셀****
forEach로 사용자 정보 객체 리스트 불러와서 index를 페이지로 하고 정보 띄우기

****Nav/Side bar****
links.classList.toggle("show-links")를 사용하면 클릭 이벤트 발생 시 show-links가 나타나거나 사라진다. 이를 통해서 show-links class에 css를 추가해 해당 클래스가 생기면 bar가 나타나게 만듬.

****Modal****
클릭하면 페이지 나타나는 화면
띄울 페이지의 css visibility값을 hidden으로 두고, open버튼을 클릭시 visibility: visible값을 가지고 있는 open-modal 클래스를 modal의 classList에 추가한다. 닫을 때는 remove로 해당 open-modal 클래스 지우기. nav,side bar과 유사하게 css를 통해 화면을 끄고 키는 형식

****question****
각 버튼들의 class를 동일하게 하고 forEach를 통해 구별 후 toggle "show-text"로 구현

****Menu****
메뉴 나열 방법 : 메뉴 정보를 담은 객체를 모아놓은 배열을 만들고,
이벤트 리스너의 DomContentLoaded를 사용해 Dom트리가 갖춰지면 map함수를 통해 화면에 페이지를 나열하도록 코딩.
![[첨부 파일 소스/Image File 1/Pasted image 20240503143209.png]]

일반적으로 나열하는 것 이외에도 dynamic으로 배열에 있는 category에 맞춰 기능을 하는 버튼이 생성되게 하는 방법.

![[첨부 파일 소스/Image File 1/Pasted image 20240503202027.png]]
**리스트를 생성하는 함수**와
**버튼 생성하고 & 카테고리에 맞춰 리스트를 추려내는 함수**
**총 2개**를 만든다.

![[첨부 파일 소스/Image File 1/Pasted image 20240503202045.png]]
받아온 리스트에 맞춰 html에 넣어주는 함수

![[첨부 파일 소스/Image File 1/Pasted image 20240503202321.png]]
버튼 생성해서 html에 넣어주는 부분

![[첨부 파일 소스/Image File 1/Pasted image 20240503202615.png]]
클릭 이벤트 생성하고 filter로 리스트 걸러서 메뉴 생성함수에 인자로 넣어주기

처음에는 리스트 생성해서 보여주는 함수, 리스트 거르고 그거에 맞게 생성하는 함수 이렇게 2개로 착각하고 진행했는데 다 끝나고 정리해보니 그게 아니였다.
하나는 생성해주는 함수고 두번째는 리스트를 추려서 생성함수를 불러와서 거기에 넣어주는 함수였다. 그게 내부에서 이루어져서 두번째 함수도 메뉴판을 만들어주는 함수로 헷갈렸었음.

**무작정 코드를 치지 말고 이 변수나 함수가 무슨 역할을 하는지 생각하면서 하자**
코드는 마법을 부리는 것이 아니라 내가 원하는 기능을 대변해주는 지시어에 불과함.

***Scroll***
Dom.getBoundingClientRect()를 통해서 페이지에서 요소의 좌표값을 받고, 해당 좌표값에 따라 어떻게 동작할지, 어떤 좌표로 페이지를 이동시킬지 구성.