사각형의 넓이를 세로로 계~속 무한대로 쪼개면
그 조각이 하나의 세로 선분에 가깝게 된다.
그 선분의 넓이는 무한소 0 -> 너무 작기 떄문에

그 선분으로 사각형이 채워지게 되면 사각형의 넓이는
무한소 X 무한대가 되어 하나의 면적을 이루게 된다.

![[Pasted image 20230917015719.png]]
두 점 사이의 면적을 구해보자

![[Pasted image 20230917015833.png]]
사각형을 채워넣으면 하방 면적의 근사값이 구해진다.
사각형의 넓이는 델타X * f(x)로 구하고 사각형의 개수만큼 곱한다.
하방면적에 최대한 가까운 넓이를 구하기 위해 사각형의 넓이를 무한대로 쪼개보자

![[Pasted image 20230917020020.png]]

무한대로 줄이면 하나의 사각형 밑변은 dx(거의 0 무한소)까지 수렵하게 된다.
  
![[Pasted image 20230917020202.png]]
그럼 이 면적의 넓이를 구하려면 ?
f(x) * dx의 값을 a ~ b 까지 곱해준다. 그럼 바로 이것이 적분.
화면에 S 처럼 생긴 것이 적분 기호.
